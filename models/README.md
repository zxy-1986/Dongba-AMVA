# Pretrained models

Place pretrained checkpoints here. Model files are intentionally not committed.

Expected default directories:

```text
models/
├── chinese-clip-vit-base-patch16/
└── Qwen2.5-VL-7B-Instruct/        # optional; only for the Qwen baseline
```

Both paths can be overridden with command-line arguments.
